Rename the CLI entry point rt → rongorongo, and guard the optional engine import

rt was too collision-prone; rongorongo is unambiguous. __init__ eagerly
imported a session module the engine does not ship, breaking the import
though the CLI never uses it — now optional, sys.path shim dropped.
